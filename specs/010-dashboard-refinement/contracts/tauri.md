# Tauri Commands: System & Filesystem

## `check_path_exists`

Checks if a given path exists and is a directory.

**Arguments**:

- `path`: `string`

**Returns**:

- `boolean`

## `ping_server` (Existing)

Already implemented. Dashboard will use it to ping `rwr.runningwithrifles.com`.
